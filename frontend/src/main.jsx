import React from 'react'; import {createRoot} from 'react-dom/client'; import {ClerkProvider} from '@clerk/react'; import './style.css'; import App from './App'; import AppErrorBoundary from './components/AppErrorBoundary';
const app=<AppErrorBoundary><App/></AppErrorBoundary>;const key=import.meta.env.VITE_CLERK_PUBLISHABLE_KEY;const validClerkKey=key?.startsWith('pk_');const clerkAppearance={layout:{socialButtonsPlacement:'top',socialButtonsVariant:'iconButton'}};createRoot(document.getElementById('root')).render(validClerkKey?<ClerkProvider afterSignOutUrl="/" appearance={clerkAppearance}>{app}</ClerkProvider>:app);
