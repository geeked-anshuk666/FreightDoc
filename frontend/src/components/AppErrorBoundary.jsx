import {Component} from 'react';
export default class AppErrorBoundary extends Component {constructor(props){super(props);this.state={failed:false}}static getDerivedStateFromError(){return {failed:true}}componentDidCatch(error){console.error('FreightDoc UI failure',error)}render(){return this.state.failed?<main className="recovery"><p className="eyebrow">DISPLAY RECOVERY</p><h1>Your shipment data arrived, but the screen could not render it.</h1><p>Refresh to return to the shipment desk. Details are available in the browser console.</p><button className="primary" onClick={()=>window.location.reload()}>Return to shipment desk</button></main>:this.props.children}}
