[pytest]
asyncio_mode = strict
markers =
    unit: deterministic pure unit tests
    integration: tests using an in-process database or dependency overrides
    contract: HTTP/schema contract tests
    live: tests requiring explicitly configured external services
addopts = -ra
